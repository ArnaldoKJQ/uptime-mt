name: Uptime Monitor

on:
  schedule:
    # Run every 5min
    - cron: '*/5 * * * *'

jobs:
  ping_site:
    runs-on: ubuntu-latest
    name: Uptime Check
    steps:
    - name: Pintg Site
      id: hello
      uses: srt32/uptime@master
      with:
        url-to-hit: "https://www.google.com"
        expected-statuses: "200"
